import { Grid } from '@mui/material';
import React from 'react';

const Profile = () => {
  return (
    <Grid container>
      <Grid item xs={12}>
        general profile info
      </Grid>
    </Grid>
  );
};

export default Profile;
